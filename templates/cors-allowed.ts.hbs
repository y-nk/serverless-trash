import type { VercelRequest, VercelResponse } from '@vercel/node'

import { allowCors } from '../lib'

export default allowCors((req: VercelRequest, res: VercelResponse) => {
  res.status(200).end('OK')
})
